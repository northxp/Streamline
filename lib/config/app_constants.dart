/// A class to hold global application constants.
/// This prevents magic strings and ensures consistency across the app.
class AppConstants {
  // Private constructor so this class cannot be instantiated.
  AppConstants._();

  /// The unique identifier for the institute, used in API calls.
  static const String instituteId = "INID2201J000001";
}